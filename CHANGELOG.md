# 更新日志

本项目的所有重大变更都将记录在此文件中。

## [1.0.0] - 2026-04-13

### 新增
- **应用图标**: 使用 mmx 生成了专业的应用图标（Claude 主题色 #d4916e），支持 ICO、ICNS、PNG 格式。
- **自定义对话框**: 在渲染进程中实现了 `showInputDialog`、`showAlert` 和 `showConfirm`，解决了 Electron 环境下原生 `alert`/`prompt` 不受支持的问题，并统一了视觉风格。
- **配置模板**: 内置了包含 13 个推荐环境变量的完整 Claude Code 配置模板。
- **自动备份**: 在激活新配置前自动对 `settings.json` 进行带时间戳的备份。
- **跨平台支持**: 从原有的 HTA (仅 Windows) 重构为 Electron 应用，支持 Windows、macOS 和 Linux。

### 变更
- **完全中文化**: 
  - 界面所有按钮（保存、新建、删除、激活、刷新等）和标签（配置名称、基础地址、API 密钥等）均已改为中文。
  - 状态文本（如“配置已同步”）中文化。
  - 窗口菜单栏（文件、编辑、视图、帮助）及其子菜单完全中文化。
- **UI 风格**: 完全保留并优化了原 HTA 版本的经典设计。

### 修复
- **Electron 安装**: 通过设置 `ELECTRON_MIRROR` 解决了网络环境下二进制文件下载失败的问题。
- **对话框崩溃**: 修复了由于调用原生 `prompt()` 导致的 UI 线程挂起/错误。
- **图标显示**: 修复了桌面快捷方式无法显示正确图标的问题。

## [0.1.0] - 2026-04-12
- 初始版本：从 HTA 逻辑重写为 Node.js/Electron 核心逻辑。
